<%= render 'mail_header' %>
<h5>Foi publicado um novo comentário na <a href="<%= @blog.base_url%>"><%= @blog.blog_name %></a>, em resposta a <a href="<%= @comment.article.permalink_url %>"><%=h(@comment.article.title) %></a>.</h5>
<h2><%=h @comment.title %></h2>
<cite><strong><%= (@comment.url.blank?) ? h(@comment.author) : link_to(h(@comment.author), @comment.url) %></strong></cite>
<%= link_to('said', @comment.permalink_url) %>
<%= display_date_and_time @comment.created_at %><br/>
<%= html @comment %>
<%= render 'mail_footer' %>
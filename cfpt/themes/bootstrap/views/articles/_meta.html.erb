<p class="text-muted text-right"><small><%= t(".published_on") + " " + display_date_and_time(article.published_at) %><%= "<br/>" + t('.comments', count: article.published_comments.size) if article.allow_comments? %></small><%= "<br/>" + article.tags.map { |tag| link_to(("<button type='button' class='btn btn-xs btn-info'>" + tag.display_name + "</button>").html_safe, tag.permalink_url(nil, true), rel: "tag")}.sort.join(" ") unless article.tags.empty? %></p>